version: '3.8'

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: dicoevent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      timeout: 10s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-h!c$_r@w%ab*k&7&&3c6)kjt!j1jub*_wa^jt_u22ahwx^o@oi
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - DATABASE_NAME=dicoevent
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - MINIO_ENDPOINT_URL=minio:9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET_NAME=dicoevent-uploads
      - MINIO_USE_SSL=False
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://redis:6379/0
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USER=your-email@gmail.com
      - MAIL_PASSWORD=your-app-password
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery_worker:
    build: .
    command: celery -A DicoEvent worker --loglevel=info
    volumes:
      - .:/app
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-h!c$_r@w%ab*k&7&&3c6)kjt!j1jub*_wa^jt_u22ahwx^o@oi
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - DATABASE_NAME=dicoevent
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - MINIO_ENDPOINT_URL=minio:9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET_NAME=dicoevent-uploads
      - MINIO_USE_SSL=False
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://redis:6379/0
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USER=your-email@gmail.com
      - MAIL_PASSWORD=your-app-password
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      web:
        condition: service_started

  celery_beat:
    build: .
    command: celery -A DicoEvent beat --loglevel=info
    volumes:
      - .:/app
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-h!c$_r@w%ab*k&7&&3c6)kjt!j1jub*_wa^jt_u22ahwx^o@oi
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - DATABASE_NAME=dicoevent
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - MINIO_ENDPOINT_URL=minio:9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET_NAME=dicoevent-uploads
      - MINIO_USE_SSL=False
      - REDIS_HOST=redis
      - CELERY_BROKER_URL=redis://redis:6379/0
      - MAIL_HOST=smtp.gmail.com
      - MAIL_PORT=587
      - MAIL_USER=your-email@gmail.com
      - MAIL_PASSWORD=your-app-password
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      web:
        condition: service_started

volumes:
  postgres_data:
  minio_data:
  redis_data: